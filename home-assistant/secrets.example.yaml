# Copy this file to secrets.yaml and replace placeholder values.
# Keep the real secrets.yaml out of Git.

lutron_caseta_bridge_ip: 192.0.2.10
lutron_caseta_keyfile: /config/lutron/caseta.key

# Example placeholders for integrations or helpers that may need secrets later.
govee_api_key: REPLACE_ME_IF_NEEDED
